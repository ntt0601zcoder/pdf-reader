# Allow the Google Identity Services sign-in popup to talk back to the opener
# (mirrors the Vite dev server header). Without this the browser logs
# "Cross-Origin-Opener-Policy policy would block the window.closed call".
/*
  Cross-Origin-Opener-Policy: same-origin-allow-popups
